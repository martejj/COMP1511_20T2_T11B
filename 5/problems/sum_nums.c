#include <stdio.h>

int sum_nums(int a, int b) ;

int main(void) {

    int a = 10;
    int b = 20;
    int sum = 5;
    
    sum = sum_nums(a, b);

    return 0;
}

int sum_nums(int a, int b) {
    return a + b;
}
scanf("%d %d %d %lf", &a, &b, &c, &d);
